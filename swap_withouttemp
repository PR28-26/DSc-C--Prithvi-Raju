/*Prithvi Raju
ISP*/
#include <stdio.h>
void main()
{
    int x, y;
    printf("enter first variable: ");
    scanf("%d", &x);
    printf("enter second variable: ");
    scanf("%d", &y);
    
    x = x + y;
    y = x - y;
    x = x - y;
    
    printf("after swapping, x = %d and y = %d", x, y);
}
